// Não usado mais — o alerta de email agora usa ntfy.sh direto (veja
// src/components/GmailAlert.tsx). Este arquivo pode ser apagado com
// segurança; deixei aqui só pra não quebrar nada caso algo ainda aponte
// pra ele.
